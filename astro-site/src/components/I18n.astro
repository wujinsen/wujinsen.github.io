---
interface Props {
  en: string;
  zh: string;
  as?: keyof HTMLElementTagNameMap;
  class?: string;
}

const { en, zh, as: Tag = 'span', class: className } = Astro.props;
---

<Tag class={className} data-en={en} data-zh={zh}>{en}</Tag>
